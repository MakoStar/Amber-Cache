name: Action test

on:
  workflow_dispatch:

jobs:
  test-job:
    # runs-on: ubuntu-latest
    runs-on: windows-latest

    steps:
      - name: 🔍 Checkout repository
        uses: actions/checkout@v6
        with:
          path: 'source_repo'
          
      # - name: 🐍 Setup python
      #   uses: actions/setup-python@v6
      #   with:
      #     python-version: "3.14"

      # - name: 📌 Run test
      #   run: |
      #     cd source_repo 

      #   shell: bash
      - name: 📌 Run test
        working-directory: source_repo
        run: ./test_manifest/ManifestTool.exe
        shell: pwsh
